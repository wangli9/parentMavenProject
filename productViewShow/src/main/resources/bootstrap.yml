spring:
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled:  true
        serviceId:  childConfigService
eureka:
  client:
    serviceUrl:
      defaultZone:  http://127.0.0.1:8080/eureka/