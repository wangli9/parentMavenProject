#	server:
#   port: 因为会启动多个 product-data-service, 所以端口号由用户自动设置，推荐 8001,8002,8003

spring:
  application:
    name: productDateService
  zipkin:
    base-url: http://127.0.0.1:9411
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8080/eureka/